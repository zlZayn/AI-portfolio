[project]
name = "ai-portfolio"
version = "0.1.0"
description = "Self-contained HTML portfolio showcasing AI projects with editorial SVG diagrams"
requires-python = ">=3.12"
dependencies = ["jinja2", "pyyaml"]
